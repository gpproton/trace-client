import type { RouterConfig } from '@nuxt/schema'

export default <RouterConfig>{
  routes: (_routes) => [
    // TODO: Apply route for possible custom usage
    // {
    //   name: 'home',
    //   path: '/',
    //   component: () => import('@/pages/home.vue').then(r => r.default || r)
    // }
  ],
}
